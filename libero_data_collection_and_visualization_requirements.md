# LIBERO 双策略实验：数据采集与可视化需求（Agent 执行版）

## 1. 文档目标

本文件只关注 **数据采集**，暂不考虑训练与评估。

目标是在 **LIBERO 仿真环境**中采集一组具有**明显双策略分叉**的数据，用于后续验证：

> 在相同或近似相同的观测与指令条件下，机器人是否会出现两种明显不同但都成功的动作路径。

同时，本阶段必须输出**充分的可视化与相似性分析结果**，以确认采集到的数据确实满足“多峰动作分布”的实验需求。

---

## 2. 本阶段需要完成的事情

Agent 需要完成以下工作：

1. 设计一个在 LIBERO 中可执行的双策略任务
2. 生成 5 组配对环境
3. 每组采集两条成功轨迹：
   - 策略 A
   - 策略 B
4. 保存轨迹、环境信息、成功 / 碰撞标记
5. 输出高质量可视化结果
6. 输出分叉前相似性、分叉后差异性分析结果

最终目标不是“尽可能多采数据”，而是：

> **先得到一小批结构清晰、解释性强、可人工检查的数据。**

---

## 3. 核心采集目标

需要采集的数据必须满足：

- **同一任务**
- **同一语言指令**
- **同组环境一致**
- **分叉前状态尽量一致**
- **分叉后路径明显不同**
- **两条路径都能成功**
- **中间路径尽量不可行**
- **成功判定必须考虑障碍碰撞**

换句话说，需要人为构造：

> **同一观测 + 同一指令 → 两种不同的成功动作路径**

这正是后续“多峰动作分布”假设的基础。

---

## 4. 任务设计要求

### 4.1 推荐任务形式

推荐采用以下结构：

- 抓取目标物体
- 绕过障碍物
- 将物体移动到目标区域

推荐双策略：

- **策略 A**：从障碍物左侧绕行
- **策略 B**：从障碍物右侧绕行

### 4.2 任务必须满足的性质

#### (1) 同一任务、同一指令

所有演示必须使用**完全相同**的文本指令，例如：

- `pick up and move the object to the target area`

不能使用任何能泄露策略信息的指令，如：

- `move from left`
- `move from right`

#### (2) 必须存在统一参考分叉点

所有轨迹都必须先执行相同的前缀动作，先移动到一个统一的参考分叉位置，然后再分成 A / B 两种策略。

即轨迹结构应近似为：

```text
初始状态
   ↓
抓取
   ↓
移动到统一参考分叉点
   ↓
A / B 分叉绕行
   ↓
到达目标区域并完成放置
```

#### (3) 分叉必须明显

A / B 不能只是轻微扰动，而必须是**肉眼可见的不同路径**。

例如：

- A 明显从障碍左边绕过去
- B 明显从障碍右边绕过去

#### (4) 中间路径尽量失败

希望中间直线路径会因撞击障碍而失败，避免出现“任意走都能过”的情况。

#### (5) 成功判定必须加入碰撞约束

即使物体最终到了目标区域，只要中途发生了与障碍物的违规碰撞，也不能算成功。

---

## 5. 数据规模与组织形式

### 5.1 初始规模

先做一个小规模验证版本：

- 共 **5 组环境**
- 每组采集：
  - 1 条 A 轨迹
  - 1 条 B 轨迹

总计：

- `A1 ~ A5`
- `B1 ~ B5`

共 **10 条轨迹**

### 5.2 配对原则

对于同组 `Ai` 和 `Bi`：

- 初始环境配置必须一致
- 指令必须一致
- 分叉前动作应尽量一致
- 分叉后策略不同

对于不同组：

- 允许做小幅环境扰动，以保证有限泛化
- 但不要大幅改变任务结构

推荐变化项：

- 目标物体初始位置轻微变化
- 障碍物位置轻微变化
- 目标区域位置轻微变化
- 初始机械臂姿态轻微变化

---

## 6. 轨迹采集要求

## 6.1 每条轨迹需要记录的内容

每条轨迹至少应保存以下信息：

### 基础信息

- `group_id`
- `trajectory_id`
- `strategy_label`（A 或 B）
- `instruction`
- `random_seed`
- `environment_config`

### 轨迹数据

- 观测序列（图像 / 状态）
- 动作序列
- 关键时间步索引
- 轨迹长度

### 关键标记

- 是否成功完成任务
- 是否发生障碍碰撞
- 碰撞发生时刻（若有）
- 分叉点时间步或参考状态索引
- 抓取完成时间步
- 放置完成时间步

### 可选附加信息

- 末端执行器位姿序列
- 目标物体位姿序列
- 障碍物位姿
- 关键事件日志

---

## 6.2 推荐保存格式

建议同时保存两类数据：

### 方式 A：轨迹文件

每条轨迹保存为 `.hdf5`，用于后续训练或回放。

### 方式 B：元信息文件

额外保存一个结构化元信息文件（如 `.json`），记录：

- 该轨迹属于哪一组
- 属于 A 还是 B
- 分叉点在哪
- 是否成功
- 是否发生碰撞
- 环境参数是什么

这样便于后续分析与人工检查。

---

## 7. 采集质量强约束

采集完成后，必须检查以下约束是否成立：

### 7.1 同组内分叉前相似

对于每组 `Ai / Bi`：

- 分叉点之前的观测应尽量相似
- 机械臂状态应尽量接近
- 物体 / 障碍物相对布局应尽量一致

### 7.2 同组内分叉后明显不同

对于每组 `Ai / Bi`：

- A 必须走一条清晰路径
- B 必须走另一条清晰路径
- 两者差异必须能够被可视化明确展示

### 7.3 两条轨迹都成功

- `Ai` 必须成功
- `Bi` 必须成功

### 7.4 中间路径不应天然可行

需要从任务设计与回放中验证：

- 直线路径是否会撞击障碍
- 任务是否真的存在“必须绕行”的约束

### 7.5 成功不允许伴随违规碰撞

若发生与障碍物的违规碰撞，即使最终完成放置，也应视作失败或单独标记为无效样本。

---

## 8. 可视化要求（非常重要）

本阶段中，可视化不是附属工作，而是**核心验收内容**。

Agent 必须输出足够清晰的可视化结果，让人能直接判断：

- 分叉前是否一致
- 分叉后是否明显不同
- 路径是否真的绕开了障碍
- 是否存在碰撞
- 该组数据是否满足实验要求

---

## 9. 必须输出的可视化内容

### 9.0 视频格式硬约束（新增）

本阶段所有动态可视化必须输出为 `.mp4` 文件。  
`gif` 仅可作为中间调试产物，不作为最终交付格式。

必须满足：

- 单轨迹回放输出 `.mp4`
- A/B 并排对比输出 `.mp4`
- 均值动作或碰撞调试动画输出 `.mp4`

---

### 9.1 单轨迹回放可视化

对每条轨迹输出可视化内容，例如：

- `mp4` 视频回放
- 关键帧拼图
- 轨迹动画

要求能看到：

- 抓取过程
- 分叉前到达参考点
- 分叉后绕行路径
- 最终放置结果

---

### 9.2 A/B 配对对比可视化

对每组 `Ai / Bi` 必须输出对比可视化，便于直接比较：

- 同组环境是否一致
- 分叉前是否相似
- 分叉后路径是否明显不同

推荐形式：

- 左右并排 `mp4` 视频
- 同步关键帧对比图
- 统一视角下的路径叠加图

---

### 9.3 俯视轨迹图 / 路径叠加图

需要生成每组的路径可视化，例如：

- 末端执行器二维轨迹图
- 目标物体运动轨迹图
- A / B 两条路径叠加展示
- 障碍物和目标区域位置同时绘制

目标是让人一眼看到：

- A 在左边绕
- B 在右边绕
- 中间区域被障碍阻挡

---

### 9.4 分叉点对齐可视化

必须输出一类专门用于检查“统一分叉前状态”的可视化结果。

建议形式：

- `Ai` 与 `Bi` 在分叉点前若干帧的同步关键帧对比
- 分叉点时刻末端位姿、抓取状态、物体位置的表格对比
- 分叉点附近视觉特征 / 状态差异指标

目的：

- 验证分叉点前两条轨迹确实足够相似
- 支撑“同一观测条件下动作不同”的实验设定

---

### 9.5 碰撞可视化 / 事件标注

如果存在碰撞检测模块，建议同时输出：

- 碰撞发生时间点
- 碰撞对象
- 碰撞时对应画面
- 碰撞样本与无碰撞样本对比

即使最终保留下来的数据都应为“无违规碰撞成功样本”，  
也建议保留调试可视化，以证明碰撞判定逻辑可信。

---

## 10. 相似性与差异性分析要求

除了可视化，还需要定量分析每组 A/B 数据是否满足实验意图。

### 10.1 分叉前相似性分析

建议分析以下内容之一或多种：

- 分叉前末端轨迹距离
- 分叉前关键状态差异
- 分叉前图像特征距离
- 分叉前动作序列距离

目标：

- 证明同组 A/B 在分叉前确实近似一致

### 10.2 分叉后差异性分析

建议分析：

- 分叉后轨迹形状差异
- 分叉后动作序列差异
- 左 / 右绕行的空间分离程度
- 分叉后关键帧视觉差异

目标：

- 证明同组 A/B 在分叉后确实显著不同

### 10.3 输出形式

建议输出：

- 每组一份分析结果
- 一张总表汇总 5 组数据
- 必要时附代表性图表

---

## 11. 推荐目录结构

建议 agent 输出如下结构：

```text
data_collection_outputs/
├── trajectories/
│   ├── group_01_A.hdf5
│   ├── group_01_B.hdf5
│   ├── ...
├── metadata/
│   ├── group_01_A.json
│   ├── group_01_B.json
│   ├── ...
├── visualizations/
│   ├── rollout_videos/
│   │   ├── group_01_A.mp4
│   │   ├── group_01_B.mp4
│   ├── paired_comparisons/
│   │   ├── group_01_A_vs_B.mp4
│   ├── topdown_paths/
│   ├── fork_alignment/
│   ├── collision_debug/
│   │   ├── group_01_mean_action_triplet.mp4
├── analysis/
│   ├── per_group_similarity.json
│   ├── summary_table.csv
│   ├── summary_report.md
```

---

## 12. Agent 需要直接给出的交付内容

请直接输出一个可执行的数据采集方案，至少包含：

### 12.1 任务方案

- 使用哪个 LIBERO 任务或如何自定义任务
- 障碍物如何设置
- 如何确保左右绕行都可成功
- 如何确保中间路径尽量失败
- 如何定义统一参考分叉点

### 12.2 采集脚本方案

- 如何生成 5 组配对环境
- 如何分别采集 A / B 两种策略轨迹
- 如何保证同组环境一致
- 如何记录分叉点、成功标记、碰撞标记
- 如何保存 `.hdf5` 与元信息

### 12.3 可视化脚本方案

- 单轨迹回放可视化
- A/B 配对对比可视化
- 俯视路径叠加图
- 分叉点对齐检查图
- 碰撞调试可视化

### 12.4 分析脚本方案

- 分叉前相似性分析
- 分叉后差异性分析
- 汇总结果表格输出

---

## 13. 验收标准

如果数据采集阶段做得合格，应该满足：

1. 有 5 组 A/B 配对轨迹
2. 每组 A/B 使用相同任务、相同指令、相同环境配置
3. 分叉前状态接近
4. 分叉后路径明显不同
5. 两条轨迹都成功
6. 成功判定包含碰撞检查
7. 有完整元信息记录
8. 有清晰可视化结果可供人工验收
9. 有相似性 / 差异性分析结果支撑实验设定

---

## 14. 最重要的提醒

本阶段最重要的不是“多采数据”，而是：

> **先做出一批能够清楚证明“同一观测下存在两种成功动作路径”的高质量样本。**

因此，agent 在设计与实现时，应优先保证：

- 任务可解释
- 分叉清晰
- 可视化充分
- 结果便于人工检查

而不是一开始追求复杂度或大规模。
